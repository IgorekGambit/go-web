{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Attach to Docker (delve)",
        "type": "go",
        "request": "attach",
        "mode": "remote",
        "remotePath": "${workspaceFolder}",
        "port": 40000,
        "host": "127.0.0.1" // TODO: убедиться, что я не иду в traefik
      }
    ]
  }